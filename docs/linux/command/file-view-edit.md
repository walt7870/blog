# 文件查看与编辑

Linux 下常用的文件查看与编辑命令有：

## 文件查看
- `cat`：连接并显示文件内容。
- `tac`：反向显示文件内容。
- `less`/`more`：分页显示文件内容。
- `head`/`tail`：显示文件的前/后若干行。
- `nl`：显示带行号的文件内容。

## 文件编辑
- `vim`/`vi`：功能强大的文本编辑器。
- `nano`：简单易用的终端文本编辑器。
- `gedit`：图形界面文本编辑器。

### 示例
```bash
cat file.txt
less /var/log/syslog
vim test.txt
```
